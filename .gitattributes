# 줄바꿈 통일: Windows(CRLF) / macOS(LF) 혼용으로 diff 가 전부 바뀌는 사고 방지
* text=auto eol=lf

# Windows 전용 스크립트는 CRLF 유지
*.ps1  text eol=crlf
*.bat  text eol=crlf
*.cmd  text eol=crlf

# 바이너리
*.hex  binary
*.bin  binary
*.elf  binary
*.png  binary
*.pdf  binary
